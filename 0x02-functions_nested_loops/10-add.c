#include "main.h"
/**
*add -  adds two integers and returns the result
*@a: first computation number
*@b: second computation number
*Return: integer value
*/
int add(int a, int b)
{
	return (a + b);
}

